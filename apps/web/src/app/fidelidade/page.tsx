export default function FidelidadePage() {
  // This page is a shell — SecaoFidelidade is rendered inside the authenticated
  // dashboard layout which injects barCodigo and token via context/props.
  return null;
}
