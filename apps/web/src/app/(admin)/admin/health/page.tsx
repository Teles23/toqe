import { HealthView } from "@/features/super-admin/components/HealthView";

export const metadata = { title: "Health — Super Admin" };

export default function AdminHealthPage() {
  return <HealthView />;
}
