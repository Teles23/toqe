import { TenantsList } from "@/features/super-admin/components/TenantsList";

export const metadata = { title: "Barbearias — Super Admin" };

export default function AdminBarbeariasPage() {
  return <TenantsList />;
}
