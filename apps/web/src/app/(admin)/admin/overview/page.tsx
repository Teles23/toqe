import { OverviewView } from "@/features/super-admin/components/OverviewView";

export const metadata = { title: "Visão Geral — Super Admin" };

export default function AdminOverviewPage() {
  return <OverviewView />;
}
