// Re-export do Stack interno do perfil — implementação compartilhada com (barbeiro).
// DRY: a tela em si detecta o grupo via useSegments() para navegação correta.
export { default } from "../../(barbeiro)/perfil/_layout";
